import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

export default function CardIcon(props) {
    return (
        <FontAwesomeIcon icon={props.icon} className="text-4xl text-indigo-500"/>
    )
}